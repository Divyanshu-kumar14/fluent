/**
 * TTS Constants
 *
 * Shared constants used across the TTS feature for text input
 * validation and cost estimation.
 */

/** Maximum number of characters allowed in a single TTS generation. */
export const TEXT_MAX_LENGTH = 5000;

/** Estimated cost per character (used for the real-time cost preview badge). */
export const COST_PER_UNIT = 0.0003;